# PostSocial — Dependências
# PRODUÇÃO

# Flask + extensões
flask>=3.0.0
flask-sqlalchemy>=3.1.0
flask-login>=0.6.3
flask-limiter>=3.5.0
python-dotenv>=1.0.0
werkzeug>=3.0.0
gunicorn>=22.0.0
flask-wtf>=1.2.0

# Criptografia (senhas Instagram)
cryptography>=42.0.0

# Instagram
instagrapi>=2.1.2

# Imagens
Pillow>=10.0.0

# PIX QR Code
qrcode[pil]>=7.4.0

# HTTP client (Facebook poster)
httpx>=0.27.0

# IA para legendas (Groq usa SDK OpenAI)
openai>=1.30.0
# anthropic>=0.40.0     # ai_provider: "anthropic" → ANTHROPIC_API_KEY
# google-genai>=1.0.0   # ai_provider: "gemini" → GOOGLE_API_KEY

# Google Drive — modo público (pasta compartilhada, sem setup)
gdown>=5.0.0

# Google Drive — modo Service Account (opcional, para pastas privadas)
google-api-python-client>=2.0.0
google-auth>=2.0.0

# Mercado Pago — pagamento automático
mercadopago>=2.2.0

# PostgreSQL
psycopg2-binary>=2.9.0
